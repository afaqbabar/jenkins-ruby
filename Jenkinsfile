pipeline {
    agent any 
    stages {
        stage('Stage 1') {
            steps {
                sh 'java -version'
                sh 'python --version'
                sh "echo "puts 'Hello World from Ruby'" > hello.rb" 
            }
        }
    }
}
