pipeline {
    agent any 
    stages {
        stage('Stage 1') {
            steps {
                sh 'java -version'
                sh 'python --version'
                sh "echo 'Hello World from Ruby'" 
            }
        }
    }
}
